import styles from '../CatalogPageTitle/CatalogPageTitle.module.scss';

const CatalogPageTitle = () => {
    return(
        <h1 className={styles.title}>Каталог</h1>
    )
}

export default CatalogPageTitle;